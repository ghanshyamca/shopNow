apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "admin.fullname" . }}-nginx-conf
  labels:
    {{- include "admin.labels" . | nindent 4 }}
data:
  default.conf: {{ .Values.nginx.config | quote }}
