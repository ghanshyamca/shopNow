apiVersion: v1
kind: Service
metadata:
  name: mongo-headless
  namespace: shopnow-demo
  labels:
    app: mongo
spec:
  type: ClusterIP
  clusterIP: None  # Headless service
  ports:
  - port: 27017
    targetPort: 27017
    protocol: TCP
    name: mongo
  selector:
    app: mongo
