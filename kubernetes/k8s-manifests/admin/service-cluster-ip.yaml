apiVersion: v1
kind: Service
metadata:
  name: admin
  namespace: shopnow-demo
  labels:
    app: admin
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: admin
